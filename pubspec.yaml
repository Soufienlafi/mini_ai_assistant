name: mini_project_genuisdk
description: "A new Flutter project."
publish_to: "none"
version: 0.1.0+1

environment:
  sdk: ^3.10.0

dependencies:
  flutter:
    sdk: flutter
  flutter_dotenv: ^5.1.0
  flutter_markdown: ^0.7.7+1
  flutter_quickalert: ^1.0.1
  google_generative_ai: ^0.4.7
  intl: ^0.20.2
  shared_preferences: ^2.2.3
 
dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^6.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/.env
